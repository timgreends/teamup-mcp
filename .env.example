# TeamUp MCP Server Configuration

# Authentication Mode: TOKEN (simple) or OAUTH (advanced)
TEAMUP_AUTH_MODE=TOKEN

# ===== TOKEN MODE Configuration (Recommended) =====
# Get these from TeamUp Settings → API → Access Tokens
TEAMUP_ACCESS_TOKEN=your-access-token
TEAMUP_PROVIDER_ID=your-provider-id

# ===== OAUTH MODE Configuration (Advanced) =====
# Get these from TeamUp Settings → Integrations → Customer API
# TEAMUP_CLIENT_ID=your-client-id
# TEAMUP_CLIENT_SECRET=your-client-secret
# TEAMUP_REDIRECT_URI=https://your-domain.com/callback

# ===== Optional Settings (Both Modes) =====
# Request mode: customer or provider
TEAMUP_REQUEST_MODE=customer

# API base URL (usually don't need to change)
# TEAMUP_BASE_URL=https://goteamup.com/api/v2

# ===== Local Development Only =====
# For local OAuth callback server
# TEAMUP_CALLBACK_PORT=8080

# For smoother OAuth flow with auth redirect service
# TEAMUP_AUTO_AUTH_URL=https://your-domain.com/auth/teamup